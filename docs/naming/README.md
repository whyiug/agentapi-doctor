# Naming Status

“AgentAPI Doctor” is a working name. No final package, OCI namespace, domain,
logo, or trademark clearance is asserted.

P00 naming research must compare at least three candidates across the GitHub
repository/organization, Go module, Python package, npm package, OCI namespace,
domain, and obvious trademark-conflict surfaces. Retrieval times, sources,
results, and limitations must be recorded; an automated availability check is
not legal clearance.

The existing `coder/agentapi` project means the generic name `agentapi` must not
be used as an ambiguous standalone CLI or package identity. A final decision
requires the P00 go/no-go and governance process.
